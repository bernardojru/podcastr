import { styled } from "../../styles/.";

export const ToggleThemesButtonContainer = styled("div", {
  button: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    background: "transparent",
  },
});
