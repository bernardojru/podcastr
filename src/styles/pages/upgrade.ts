import { styled } from "..";

export const ContainerRegister = styled("div", {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  background: "$primary",
  height: "100%",
});

export const ContainerCenter = styled("div", {
  width: "40rem",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  marginBottom: "$80",
});

export const MultiStepContainer = styled("div", {
  width: "100%",
  p: {
    color: "$text",
  },
});

export const ContainerStepLogin = styled("div", {});
